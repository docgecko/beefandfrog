- if user_signed_in?
	== Logged in as #{current_user.email}
	| 
	= link_to('Logout', destroy_user_session_path)
- else
	= link_to "Sign in as User with Facebook", user_omniauth_authorize_path(:facebook)
