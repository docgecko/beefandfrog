- content_for :title do
	= I18n.t :title, :scope => [:apartments]

#apartments
	%h2.title= (@about.title_apartments).html_safe
	= markdown(@about.content_apartments)
	
	= @apartments.each do |apartment|
		.border
			.box
				.image
				.title
					.button
				.price
				.details
				.description